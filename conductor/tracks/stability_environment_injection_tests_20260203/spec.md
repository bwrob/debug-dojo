# Specification: Stability - Environment Injection Tests

## Overview
Improve the robustness of the environment injection logic in `_installers.py` by adding tests for edge cases, different shell environments, and permission issues.
